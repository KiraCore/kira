#!/usr/bin/env bash
ETC_PROFILE="/etc/profile" && set +e && source /etc/profile &>/dev/null && set -e
# quick edit: FILE="$KIRA_MANAGER/menu/seed-select.sh" && rm $FILE && nano $FILE && chmod 555 $FILE
set +x



