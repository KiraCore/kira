# Query Address Book

```
docker exec -i seed cat "$SEKAID_HOME/config/addrbook.json"

docker exec -i sentry cat "$SEKAID_HOME/config/addrbook.json"

docker exec -i priv_sentry cat "$SEKAID_HOME/config/addrbook.json"
```